version: 2

models:
  - name: session_summary
    description: "Analytics session summary"
    columns:
      - name: sessionId
        description: "Unique session identifier"
        data_tests:
          - unique
          - not_null

